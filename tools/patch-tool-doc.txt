# Outil de patch GitHub – patch-tools.js

## 1. Objectif général
L’outil **Patch Tool** permet d’appliquer des modifications automatisées sur des fichiers d’un dépôt GitHub. Il prend en entrée un **fichier JSON de patch** décrivant des changements (`replace`, `insert`, `delete`, etc.), vérifie des **préconditions strictes**, et applique les changements soit en mode **Dry-run** (simulation), soit en mode **Apply** (écriture réelle sur GitHub).

## 2. Fonctionnalités principales
### 2.1 Dry-Run
- Simule le patch sans rien écrire sur GitHub.
- Télécharge chaque fichier cible.
- Applique virtuellement les opérations (replace, insert, delete...).
- Génère un **rapport détaillé** dans le champ de sortie (`#patch-out`).
- Affiche les logs dans la console (avec niveaux INFO, WARN, ERROR).

### 2.2 Apply
- Identique au Dry-run, mais écrit réellement les fichiers sur GitHub.
- Envoie un commit par fichier via l’API GitHub `PUT /contents/{path}`.
- Utilise un **committer** générique : `{ name: "Patch Tool", email: "noreply@example.com" }`.
- Vérifie le SHA pour assurer un **verrouillage optimiste** (évite d’écraser un fichier modifié depuis).

### 2.3 Opérations supportées
| Type | Effet |
|------|--------|
| replace | Remplace la première occurrence d’un texte. |
| replace_all | Remplace toutes les occurrences. |
| replace_regex | Remplace via une expression régulière. |
| insert_after_anchor | Ajoute des lignes après une ligne contenant une chaîne. |
| insert_before_anchor | Ajoute des lignes avant une ligne contenant une chaîne. |
| insert_after_line | Ajoute des lignes après une ligne correspondant à un regex. |
| insert_before_line | Ajoute des lignes avant une ligne correspondant à un regex. |
| delete_line | Supprime des lignes contenant un texte ou un regex. |

## 3. Format du patch JSON
Structure : 
```
{
  "changes": [
    {
      "path": "chemin/du/fichier.js",
      "pre": { ... },
      "ops": [ ... ],
      "message": "message optionnel"
    }
  ]
}
```

## 4. Vérifications et préconditions
- `expectSha` : SHA GitHub attendu.
- `expectSize` : taille du fichier.
- `expectHash` : hash SHA-256 attendu.
- `expectIncludes` : texte obligatoire dans le fichier.
- `strict` : si true, bloque sur écart ; sinon passe avec avertissement.

## 5. Aides intégrées
- Conversion UTF-8/Base64 propre (préserve emojis).
- Console intégrée (`TV.log`).
- Protection contre les caractères “smart” (guillemets courbes, tirets, NBSP...).
- Normalisation automatique du texte JSON avant parsing.
- Hardening des textarea (désactivation autocorrect, etc.).

## 6. Interface
- `#btn-patch-dryrun` : simulation.
- `#btn-patch-apply` : application réelle.
- `#patch-in` : zone JSON d’entrée.
- `#patch-out` : zone de sortie des logs.
- `📂 Load JSON file` : bouton pour importer un fichier .json local.

## 7. Sécurité
- Vérrouillage SHA GitHub.
- Hash SHA-256 calculé côté client.
- Gestion des erreurs via try/catch, `window.onerror`, et `unhandledrejection`.
- Logs détaillés pour chaque étape.

## 8. Intégration
Repose sur `TV` (fourni par app.js) : `ghBase`, `ghHeaders`, `log`.

## 9. Flux typique
1. Entrer owner/repo/branch/token.
2. Coller ou importer le JSON.
3. Lancer Dry-run.
4. Vérifier le résultat.
5. Lancer Apply.
6. Consulter la sortie et les logs.

## 10. Versions
| Version | Changements |
|----------|-------------|
| v8 | Base complète du patch tool |
| v9 | Hardening et nettoyage iPad / smart punctuation |
| v10 | Ajout du bouton 📂 Load JSON file |
